'use client'

import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#1E90FF',
    },
    background: {
      default: '#F7F7F7',
    },
  },
})

export default theme
